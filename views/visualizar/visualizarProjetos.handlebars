<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visualizar Projetos</title>
    <link rel="stylesheet" href="/path/to/your/css/file.css">
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
            text-align: left;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        tr:hover {
            background-color: #ddd;
        }
    </style>
</head>
<body>
    <h1>Visualizar Projetos</h1>
    <form action="/visualizarProjetosFiltrados" method="get">
        <div class="form-group">
            <label for="palavraChave">Filtrar por Palavra-Chave:</label>
            <select class="form-control" id="palavraChave" name="palavraChave">
                <option value="">Nenhum Filtro</option>
                {{#each palavrasChaveDisponiveis}}
                    <option value="{{this.nome}}">{{this.nome}}</option>
                {{/each}}
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Filtrar</button>
    </form>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Resumo</th>
                <th>Link Externo</th>
                <th>Palavras-Chave</th>
                <th>Alunos</th>
            </tr>
        </thead>
        <tbody>
            {{#each projetos}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.nome}}</td>
                <td>{{this.resumo}}</td>
                <td>{{this.link_externo}}</td>
                <td>
                    {{#each this.PalavrasChave}}
                        {{this.nome}}{{#unless @last}}, {{/unless}}
                    {{/each}}
                </td>
                <td>
                    {{#each this.Alunos}}
                        {{this.login}}{{#unless @last}}, {{/unless}}
                    {{/each}}
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>